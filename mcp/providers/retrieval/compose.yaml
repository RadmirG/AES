services:
  retrieval-mcp:
    image: aes-retrieval-mcp:latest
    container_name: retrieval-mcp
    restart: unless-stopped
    profiles:
      - retrieval
    ports:
      - "127.0.0.1:8004:8000"
    networks:
      - ai-stack-net

networks:
  ai-stack-net:
    external: true

