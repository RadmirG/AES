# Filesystem MCP Provider

Planned provider for narrow, permission-aware filesystem access.

This provider should not expose unrestricted shell or filesystem access. It
should be configured with explicit project roots, read/write modes, and file
extension policies.

