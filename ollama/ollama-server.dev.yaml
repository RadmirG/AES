services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama-server
    restart: unless-stopped
    ports:
      - "127.0.0.1:11435:11434"
    volumes:
      - ./data:/root/.ollama
    environment:
      - OLLAMA_CONTEXT_LENGTH=4096
    networks:
      - ai-stack-net

  ollama-model-puller:
    image: python:3.12-slim
    profiles:
      - models
    depends_on:
      - ollama
    working_dir: /workspace
    volumes:
      - ./:/workspace/ollama:ro
    environment:
      - OLLAMA_URL=http://ollama:11434
    command:
      - python
      - ollama/pull_models.py
      - --file
      - ollama/models.dev.yaml
      - --group
      - "${AES_OLLAMA_PULL_GROUP:-recommended}"
      - --include-default
      - --model
      - "${AES_OLLAMA_MODEL:-qwen3:4b}"
    networks:
      - ai-stack-net

networks:
  ai-stack-net:
    external: true

