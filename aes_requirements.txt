# Central AES Python test/development environment.
#
# Install this file into a local environment named aes_test_env when you want
# one Python environment that can run all current repository tests and helper
# scripts outside Docker.
#
# Each Python-capable subproject owns its runtime requirements.txt. This
# central file composes them and adds repository-wide development/test tools.

-r langgraph/requirements.txt
-r mcp/requirements.txt
-r ollama/requirements.txt

# Repository-wide test runner.
pytest>=8,<10
